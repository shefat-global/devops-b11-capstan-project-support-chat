services:
  backend:
    build:
      context: ./backend
    image: support-chat-backend:dev
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    image: support-chat-frontend:dev
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped