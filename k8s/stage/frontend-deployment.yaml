apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-chat-frontend
  namespace: support-chat-stage
  labels:
    app: support-chat-frontend
    environment: stage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: support-chat-frontend
  template:
    metadata:
      labels:
        app: support-chat-frontend
        environment: stage
    spec:
      containers:
        - name: frontend
          image: shiftrobin/support-chat-frontend:prod-7d684b8
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
