apiVersion: v1
kind: Service
metadata:
  name: support-chat-frontend
  namespace: support-chat-stage
  labels:
    app: support-chat-frontend
    environment: stage
spec:
  type: ClusterIP
  selector:
    app: support-chat-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
