apiVersion: v1
kind: Service
metadata:
  name: support-chat-frontend
  namespace: support-chat-dev
  labels:
    app: support-chat-frontend
    environment: dev
spec:
  type: ClusterIP
  selector:
    app: support-chat-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
